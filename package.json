{
  "name": "bharat-ticker-cloudflare",
  "version": "3.0.0",
  "private": true,
  "description": "Native Cloudflare Worker (free plan) reimplementing the bharat /api/v1/sb/* feed — real-time Groww/Tickertape quotes+intraday, Yahoo for history. No origin, no container.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
